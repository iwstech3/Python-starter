# # Assignment Todos:

# 1. Define a dictionary named `contacts` to store contact information.
# 2. Implement the `add_contact` function with parameters `name`, `phone`, and `email`.
# 3. Write the `search_contact` function to find and return contact info by `name`.
# 4. Create the `update_contact` function to modify phone and email by `name`.
# 5. Develop the `delete_contact` function to remove a contact by `name`.
# 6. Demonstrate functionality:
#     a. Add contacts for "Alice" and "Bob".
#     b. Display the `contacts` dictionary after adding.
#     c. Search for and print contact info of "Alice".
#     d. Update "Bob's" phone and email.
#     e. Show the updated `con# Assignment Todos:
# 1. Define a dictionary named `contacts` to store contact information.
# 2. Implement the `add_contact` function with parameters `name`, `phone`, and `email`.
# 3. Write the `search_contact` function to find and return contact info by `name`.
# 4. Create the `update_contact` function to modify phone and email by `name`.
# 5. Develop the `delete_contact` function to remove a contact by `name`.
# 6. Demonstrate functionality:
#     a. Add contacts for "Alice" and "Bob".
#     b. Display the `contacts` dictionary after adding.
#     c. Search for and print contact info of "Alice".
#     d. Update "Bob's" phone and email.
#     e. Show the updated `contacts` dictionary.
#     f. Delete the contact for "Alice" and display the updated `contacts`.
# 7. Test functions with different scenarios for validation.tacts` dictionary.
#     f. Delete the contact for "Alice" and display the updated `contacts`.
# 7. Test functions with different scenarios for validation.